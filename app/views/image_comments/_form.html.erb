<%= form_for(@image_comment),remote: true, html: { style: "display:inline;" } do |f| %>
  <% if @image_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_comment.errors.count, "error") %> prohibited this image_comment from being saved:</h2>

      <ul>
      <% @image_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id,class: "control-label" %>
    <%= f.number_field :user_id,class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :image_id,class: "control-label" %>
    <%= f.number_field :image_id, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :comment,class: "control-label" %><br>
    <%= f.text_field :comment, class: "form-control" %>
  </div>
  <div class="modal-footer">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
